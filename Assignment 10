1. Write a program to print the elements from List using normal for loop.
import java.util.*;

public class ListExample {
    public static void main(String[] args) {
        List<String> names = new ArrayList<>();
        names.add("Surya");
        names.add("Meghna");
        names.add("Ramya");
        names.add("Karthik");

        for (int i = 0; i < names.size(); i++) {
            System.out.println(names.get(i));
        }
    }
}

2. Write a program to store the elements in Set and print by sorting order.
import java.util.*;

public class SortedSetExample {
    public static void main(String[] args) {
        Set<String> names = new HashSet<>();
        names.add("Surya");
        names.add("Meghna");
        names.add("Ramya");
        names.add("Karthik");

        
        List<String> sortedList = new ArrayList<>(names);
        Collections.sort(sortedList);

        System.out.println("Names in sorted order:");
        for (String name : sortedList) {
            System.out.println(name);
        }
    }
}

3. Write a program to store the elements in Map <String,String> and print the elements.

java
import java.util.*;

public class MapExample {
    public static void main(String[] args) {
        Map<String, String> student = new HashMap<>();

        student.put("Name", "Surya");
        student.put("Course", "Java");
        student.put("City", "Chennai");

        System.out.println("Student Details:");
        for (Map.Entry<String, String> entry : student.entrySet()) {
            System.out.println(entry.getKey() + ": " + entry.getValue());
        }
    }
}

